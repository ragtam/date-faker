# date-faker

Package overriding what the now ( `new Date()`, `Date.now()` ) is in JS. It allows you to manually change current date with `moment.js` like syntax.

## description

Code in src. Build transpiled to dist folder, which later gets deployed to npm.

## npm

https://www.npmjs.com/package/date-faker

## contributing

All suggestions / changes / bug reports are welcome.
